[package]
name = "ncm_dump"
version = "0.1.0"
edition = "2024"

[dependencies]
json = "=0.12.4"
native-dialog = "0.9.0"
include_dir = "0.7"

[package.metadata.scripts]
run = "cargo run"
check = "cargo check"
build = "cargo build"
release = "cargo build --release"
test = "cargo test"